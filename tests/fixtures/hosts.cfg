# Test hosts.cfg for the PAGE= scoping map (offline worker tests).

page production Production servers
10.0.0.2	db1.example.com		# conn

page other Everything else
10.0.0.1	web1.example.com	# conn
10.0.0.3	ok1.example.com		# conn
10.0.0.4	ghost.example.com	# conn
